const fs = require('fs'); const content = fs.readFileSync('ProjectPage_old.jsx', 'utf8'); const startStr = 'const styles = ' + String.fromCharCode(96); const endStr = String.fromCharCode(96) + ';'; const start = content.indexOf(startStr) + startStr.length; const end = content.indexOf(endStr, start); const css = content.substring(start, end).trim(); fs.writeFileSync('src/pages/ProjectPage.css', css, 'utf8');
